<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoPratico_Mobile1">

        <!-- Activity de Login - LAUNCHER -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity de Cadastro -->
        <activity
            android:name=".ui.login.RegisterActivity"
            android:exported="false" />

        <!-- Activity Principal (Home) -->
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="false" />

        <!-- Activity de Formulário de Lista -->
        <activity
            android:name=".ui.listform.ListFormActivity"
            android:exported="false" />

        <!-- Activity de Detalhes da Lista -->
        <activity
            android:name=".ui.listdetail.ListDetailActivity"
            android:exported="false" />

        <!-- Activity de Formulário de Item -->
        <activity
            android:name=".ui.itemform.ItemFormActivity"
            android:exported="false" />

    </application>

</manifest>